# Good First Issues

- Add support for `helm`-based release validation in `06-kubernetes/rollout-restart.sh`.
- Extend `09-security/secret-scan.sh` with a custom regex allowlist format.
- Add Prometheus Pushgateway support to `08-monitoring/host-resource-check.sh`.
- Add `--json` output mode to `03-networking/port-reachability.sh`.
- Add an example systemd unit for `10-sre-usecases/auto-heal-service.sh`.
- Add GCP or Azure equivalents for the AWS scripts in `07-cloud/`.
