# Templates

This folder contains reusable Bash templates that establish repository-wide conventions for logging, validation, and error handling. Use `script_template.sh` as the starting point for new scripts and reference the standalone logging or error handling templates when you need a smaller subset of the shared patterns.
