# Projects

The `projects/` folder contains composable end-to-end examples that combine multiple operational patterns from the rest of the repository. Each project includes its own README and executable scripts so contributors can study how small Bash utilities become usable systems.
